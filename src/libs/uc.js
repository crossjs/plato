// const uc = window.Bridge.require('sdp.uc').promise()

// uc.getCurrentUser({
//   // 是否要网络获取
//   force: false
// }).then(function (res) {
//   alert(JSON.stringify(res))
// })

export default window.Bridge.require('sdp.uc').promise()
